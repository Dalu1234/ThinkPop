# Forward /generate to real MDM (e.g. brainpop on port 8001 — avoid clashing with this gateway on 8000)
MDM_SERVICE_URL=http://127.0.0.1:8001
MOTION_DEV_STUB=0

# Local stub only (no GPU MDM): comment out MDM_SERVICE_URL and use:
# MOTION_DEV_STUB=1
