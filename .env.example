# Required for live multi-agent lesson generation (optional — app uses demo data without it)
OPENAI_API_KEY=

# Optional overrides
OPENAI_MODEL=gpt-4o-mini

# Standalone API server only (npm run dev:api). Default dev is a single Vite server on 127.0.0.1:5173.
LESSON_SERVER_PORT=8787
